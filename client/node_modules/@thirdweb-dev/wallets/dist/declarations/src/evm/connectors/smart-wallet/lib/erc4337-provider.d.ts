import { ethers, Signer } from "ethers";
import { EntryPoint, UserOperationStruct } from "@account-abstraction/contracts";
import { ClientConfig } from "@account-abstraction/sdk";
import { BaseAccountAPI } from "./base-api";
import { ERC4337EthersSigner } from "./erc4337-signer";
import { HttpRpcClient } from "./http-rpc-client";
export declare class ERC4337EthersProvider extends ethers.providers.BaseProvider {
    readonly chainId: number;
    readonly config: ClientConfig;
    readonly originalSigner: Signer;
    readonly originalProvider: ethers.providers.BaseProvider;
    readonly httpRpcClient: HttpRpcClient;
    readonly entryPoint: EntryPoint;
    readonly smartAccountAPI: BaseAccountAPI;
    initializedBlockNumber: number;
    readonly signer: ERC4337EthersSigner;
    constructor(chainId: number, config: ClientConfig, originalSigner: Signer, originalProvider: ethers.providers.BaseProvider, httpRpcClient: HttpRpcClient, entryPoint: EntryPoint, smartAccountAPI: BaseAccountAPI);
    /**
     * finish intializing the provider.
     * MUST be called after construction, before using the provider.
     */
    init(): Promise<this>;
    getSigner(): ERC4337EthersSigner;
    perform(method: string, params: any): Promise<any>;
    getTransaction(transactionHash: string | Promise<string>): Promise<ethers.providers.TransactionResponse>;
    getTransactionReceipt(transactionHash: string | Promise<string>): Promise<ethers.providers.TransactionReceipt>;
    getSenderAccountAddress(): Promise<string>;
    waitForTransaction(transactionHash: string, confirmations?: number, timeout?: number): Promise<ethers.providers.TransactionReceipt>;
    constructUserOpTransactionResponse(userOp1: UserOperationStruct): Promise<ethers.providers.TransactionResponse>;
    detectNetwork(): Promise<any>;
}
//# sourceMappingURL=erc4337-provider.d.ts.map